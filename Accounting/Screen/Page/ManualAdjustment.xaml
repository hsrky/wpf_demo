<UserControl x:Class="Accounting.Screen.Page.ManualAdjustment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <DockPanel>
        <DockPanel DockPanel.Dock="Bottom" Height="56">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="8,8,8,16">
                <Button x:Name="SaveButton" Content="Save" Width="120" Height="32" Margin="0,0,8,0"/>
                <Button x:Name="CancelButton" Content="Discard" Width="120" Height="32" IsEnabled="False"/>
            </StackPanel>
        </DockPanel>

        <DockPanel DockPanel.Dock="Top" Height="120"/>
    </DockPanel>
</UserControl>
